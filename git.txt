# Workflow
git merge feature/xapi5client_develop (merge every day)
git checkout feature/xapi5client_develop
git fetch
git pull
git merge feature/MY_FEATURE_BRANCH
git status
git push

# Workflow 2
git checkout feature/xapi5client_release_2.1.0
gulp app_build --buildConfig ./config/gulp/buildConfig_deploy.json
git commit -am "[XSTATHTML-2003 off on weekends]"
git fetch
git status
git pull
git push

npm outdated // show outdated packages

(zeby usunac trzeba zcheckoutowac sie na inny branch)
git branch -D feature/xapi5client_develop usuwanie branchu localnie
usuniecie remote
git push -d origin feature/XSTATHTML-2003-loader-login

nowy branch
git checkout-b feature/XSTATHTML-2003-loader-login

git branch -a // lista branchow

git diff origin/master --name-only

show compare head with past commit
git diff 3ee215870a92b4bdd0902e3927a16c1e52923d9c app/index.template.html

show ignored files
git status --ignored

checkout na origin tak samo:
git checkout feature/xapi5client_release_2.2.0

checkout na previous commit (dont forget .)
git checkout ab8b2900227fc305d2f00eecd79f7f582e191685 .

get origin url
git config --get remote.origin.url

change origin url
git remote set-url origin https://github.com/USERNAME/REPOSITORY.git

This will show the entire history of the file (including history beyond renames and with diffs for each change).
git log --follow -p -- file
